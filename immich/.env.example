# Pin to the version the database was migrated by. Upgrading is a one-way
# migration - bump this deliberately, never rely on :release.
IMMICH_VERSION=v1.120.2

DB_HOSTNAME=immich_postgres
DB_USERNAME=postgres
DB_DATABASE_NAME=immich
DB_PASSWORD=          # recovered from portainer.db - see CLAUDE.md
REDIS_HOSTNAME=immich_redis
